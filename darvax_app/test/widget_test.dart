import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App loads', (WidgetTester tester) async {
    // Placeholder — app requires ProviderScope setup
    expect(1 + 1, 2);
  });
}
